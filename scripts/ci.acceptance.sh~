#!/bin/bash
# script to execute acceptance tests in the CI environment

function acceptanceTest(){

./test.acceptance.sh
return $?

}

# import libtest
source  $(dirname $(readlink -f $0))/libtest.sh
main $*
exit $?

##################### DEPLOYMENT TABLE ############################
#WARNING: No borrar la línea anterior, ya que es el separador
# del script y de la DEPLOYMENT TABLE
#--------------+--------------------------------------------------------------
# Enviroment   | URL 
#--------------+--------------------------------------------------------------
int            | int-tdafsprayer-01.hi.inet
qa             | qa-tdafsprayer-01.aislada.hi.inet
dev            | localhost
